# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0,<1.0.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
slowapi>=0.1.9,<1.0.0

# ── Auth & Security ────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0.0

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9,<3.0.0
sqlalchemy>=2.0.28,<3.0.0

# ── Caching & Streaming ────────────────────────────────────────────────────────
redis>=5.0.3,<6.0.0
confluent-kafka>=2.3.0,<3.0.0

# ── ML / FraudSense ───────────────────────────────────────────────────────────
xgboost>=2.0.3,<3.0.0
# shap<0.46 uses lightgbm/sklearn feature importance — NO numba/llvmlite dependency
shap>=0.44.0,<0.46.0
scikit-learn>=1.4.1,<2.0.0

# ── LangChain / FinLens / RegGuard ────────────────────────────────────────────
# Pin to 0.3.x family — 0.4.x+ is unreleased/unstable as of 2025
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-groq>=0.2.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
groq>=0.9.0,<2.0.0
# langgraph 0.3+ introduced langgraph-sdk heavy deps — stay on 0.2.x LTS
langgraph>=0.2.0,<0.3.0

# ── Data / Parsing ────────────────────────────────────────────────────────────
pandas>=2.0.0,<3.0.0
pypdf>=4.1.0,<5.0.0
pymupdf>=1.23.26,<1.25.0
python-dotenv>=1.0.1,<2.0.0

# ── Observability ─────────────────────────────────────────────────────────────
evidently>=0.4.0,<0.5.0
# gradio 5.x is a complete rewrite with different deps — pin to 4.x explicitly
gradio>=4.26.0,<5.0.0
matplotlib>=3.8.0,<4.0.0

# ── Testing & Quality ─────────────────────────────────────────────────────────
faker>=24.0.0,<30.0.0
pytest>=8.1.1,<9.0.0
pytest-cov>=4.1.0,<6.0.0
ruff>=0.3.3,<1.0.0
mypy>=1.9.0,<2.0.0
