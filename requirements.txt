# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0,<1.0.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
slowapi>=0.1.9,<1.0.0
python-multipart>=0.0.9,<1.0.0


# ── Auth & Security ────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0.0

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9,<3.0.0
sqlalchemy>=2.0.28,<3.0.0

# ── Caching & Streaming ────────────────────────────────────────────────────────
redis>=5.0.3,<6.0.0
confluent-kafka>=2.3.0,<3.0.0

# ── ML / FraudSense ───────────────────────────────────────────────────────────
xgboost>=2.0.3,<3.0.0
scikit-learn>=1.4.1,<2.0.0
# SHAP: use 0.43.x — last version before numba became a hard dependency
shap>=0.43.0,<0.44.0

# ── LangChain / FinLens / RegGuard ────────────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-groq>=0.2.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
groq>=0.9.0,<2.0.0
# langgraph 0.2.x is fully compatible with langchain-core 0.3.x
langgraph>=0.2.0,<0.3.0
huggingface-hub>=0.24.0,<1.0.0


# ── Data / Parsing ────────────────────────────────────────────────────────────
pandas>=2.0.0,<3.0.0
pypdf>=4.1.0,<5.0.0
python-dotenv>=1.0.1,<2.0.0

# ── Observability ─────────────────────────────────────────────────────────────
# evidently 0.7.x has evidently.legacy API used in drift_detector.py
# 0.4.x does NOT have legacy API + pulls pymupdf as transitive dep
evidently>=0.7.0,<0.8.0
# gradio 4.x only — 5.x is a full rewrite
gradio>=4.26.0,<5.0.0
matplotlib>=3.8.0,<4.0.0

# ── Testing & Quality ─────────────────────────────────────────────────────────
faker>=24.0.0,<30.0.0
pytest>=8.1.1,<9.0.0
pytest-cov>=4.1.0,<6.0.0
ruff>=0.3.3,<1.0.0
mypy>=1.9.0,<2.0.0
